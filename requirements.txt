PyQt6
rasterio
pyshp
pyproj
shapely
numpy
pillow

# TCS module: runs the bundled detection model (models/*.onnx) via
# onnxruntime (CPU-only, ~15-20 MB) - no PyTorch/other large ML framework
# needed, so the app runs fully self-contained out of the box.
onnxruntime
