name: tcs-build
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - numpy
  - pillow
  - rasterio
  - pyproj
  - shapely
  - pip:
      - PyQt6
      - pyinstaller
      - pyshp
